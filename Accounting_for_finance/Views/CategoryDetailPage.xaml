<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Accounting_for_finance.Views.CategoryDetailPage" 
             Title="Информация о категориях">

    <ContentPage.ToolbarItems>

    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <StackLayout>
            <Label Text="Название" FontSize="Medium" />
            <Entry Text="{Binding Name}" d:Text="Bill name" FontSize="Small" />
            <Label Text="Выберете тип категории: доходная/расходная" FontSize="Medium" />
            <Switch IsToggled="{Binding Type}" x:Name="Cost" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>